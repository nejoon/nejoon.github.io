## git checkout:
상자를 이동하는 명령어

## git checkout -b :
내방에서 상자를 생성하는것

## git push origin:
내방에서 만든상자를 냉장고에 올리는것
단,냉장고에 상자가 없을경우 상자도 만드는 역할을 가짐 (없을 경우)

## git pull origin xxx :
냉장고에 잇는 파일을 내방에있는 파일로 가져오고싶을때,내방의 것을 최신의 것으로 반영하는것
(내방에 없는 경우)